feat:
  ☐ [v2] StringTemplateFinalString 从「整串 final」升级为「片段级字面量保护」：记录受保护片段，下一层渲染前 mask、渲染后 unmask，使 A 层故意留给 B 层的占位符仍可被展开（现状是整串保护，会误伤多阶段填充管线）
  ☐ [opt-in] 带内哨兵编码（零宽字符 / nonce 包裹）以支持跨进程、跨服务、落库、LLM 往返的标记传递; 代价：污染载荷内容，可能泄入最终输出或 prompt，需显式开关与剥离入口
bugs:
  ☐ jinja2 自定义函数（filter）不支持异步处理
  ✔ jinja2 函数返回的数组索引，这样写会报告错误：`{{ func()[-1] }}`, 必须扩起来`{{ (func())[-1] }}` @done(25-03-25 19:45)
  ☐ JinJa2 convertToRuntimeValues 如果存在自引用的对象，就会死循环: Maximum call stack size exceeded
    临时在上层检查数据是否存在这样的对象，如果有就设置为null.
